# TASKS.md

Active work only. Keep this file short.

## Current

No active task selected.

When there are no active tasks, do not invent implementation tasks directly in this file. For new projects or major features:

1. Use `prompts/ai-dev-tasks/create-prd.md` to create `tasks/prd-[feature-name].md`.
2. After the PRD is approved, use `prompts/ai-dev-tasks/generate-tasks.md` to create `tasks/tasks-[feature-name].md`.
3. Link the active generated task file here once work starts.

## Notes

- Move completed work out of this file when it is no longer useful.
- Pull future work from `BACKLOG.md`.
- Keep detailed generated task lists under `tasks/`.
- After completing a task, update `PROJECT_STATE.md`.
- If a task creates a durable decision or exception, update `DECISIONS.md`.
