# RULES.md

Project-specific rules and overrides.

These rules may add detail to global Codex rules. They must not weaken safety rules around secrets, destructive commands, or preserving user changes.

## Repo Rules

- TODO: Add project-specific conventions here.

## Commands

- Use the commands listed in `README.md`.
- If a command fails twice with the same error, stop retrying and change approach.

## Testing

- Follow the testing rules in `PROCESS.md`.
- TODO: Add project-specific test command and test placement rules.

## Exceptions

Record exceptions here only if they are active rules. Put rationale and history in `DECISIONS.md`.

- TODO: Example: Use Exa for scraping by default; use Brave only when Exa cannot access the source or returns insufficient results.
